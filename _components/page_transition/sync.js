// Make the page-transition module known to requirejs so it can be loaded
// without request into the javascript inlined in the markup.
define([
    '_components/page_transition/js/page-transition'
], function(){

});